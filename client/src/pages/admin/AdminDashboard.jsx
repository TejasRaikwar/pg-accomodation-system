import React from "react";

const AdminDashboard = () => (
  <div>
    <h1 className="text-3xl font-bold mb-6">Admin Dashboard</h1>
    <p className="mb-4">Welcome, Admin! Use the sidebar to manage users, PGs, and bookings.</p>
    {/* You can add stats, charts, or quick links here */}
  </div>
);

export default AdminDashboard;